<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Sailsjs Admin Panel</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= (req.path == '/dashboard') ? 'active' : '' %>"><a href="/dashboard">Dashboard</a></li>
        <% for(var i=0; i<topMenu.length; i++) {%>
		      <li class="<%= (req.path == '/'+topMenu[i].route) ? 'active' : '' %>"><a href="<%= topMenu[i].route %>"><%= topMenu[i].name %></a></li>
		    <% } %>        
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/logout">Logout</a></li>        
        </li>
      </ul>
      <p class="navbar-text navbar-right">Signed in as <a href="#" class="navbar-link"><%= user[0].getFullName() %></a></p>      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>